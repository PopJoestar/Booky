import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  dialogWrapper: {maxHeight: '80%'},
  scrollviewCentered: {
    flexGrow: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default Styles;
