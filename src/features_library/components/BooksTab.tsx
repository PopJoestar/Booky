import React from 'react';
import {Appbar, Box, ScrollView} from '../../shared/components';
import {Appbar as PAppbar} from 'react-native-paper';
type Props = {};

const BooksTab = (props: Props) => {
  return <Box></Box>;
};

export default BooksTab;
